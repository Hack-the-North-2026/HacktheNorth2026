{
  "name": "fit-stealer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n frontend,backend,ai -c cyan,magenta,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run dev --prefix ai-service\"",
    "install:all": "npm install --legacy-peer-deps --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
