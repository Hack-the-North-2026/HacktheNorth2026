{
  "name": "fit-stealer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n frontend,backend,ai -c cyan,magenta,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run dev --prefix ai-service\"",
    "install:all": "npm ci --legacy-peer-deps --prefix frontend && npm ci --include=optional --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "expo": "^57.0.24",
    "react": "19.2.3",
    "react-native": "0.86.3"
  }
}
