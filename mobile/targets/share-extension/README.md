# Native Share Extension Target

This directory contains placeholders and configurations for native iOS Action/Share Extension and Android Send Intent targets.

## iOS Setup
- iOS Share Extension Target (`ShareExtension/`)
- Configured with `expo-share-intent` or custom Expo Config Plugin.

## Android Setup
- Android `RECEIVE_SHARING_INTENT` intent filter configuration in `AndroidManifest.xml`.
