<?xml version="1.0" encoding="utf-8"?>
<!--
  AccessibilityService descriptor.

  Key flags explained:
  - accessibilityEventTypes="typeAllMask"  → we don't actually consume events;
    must declare something so Android accepts the service.
  - accessibilityFeedbackType="feedbackGeneric"  → generic (no audio/haptic feedback
    generated by the framework — we handle haptics ourselves).
  - canTakeScreenshot="true"  → required for takeScreenshot() on API 30+.
    Without this flag the call fails silently.
  - notificationTimeout="100"  → ms between batched events (irrelevant for us).
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.fitstealer.app.MainActivity" />
